import { <%= props.importName %> } from '<%= props.importPath %>';

export class <%= props.name %>UseCase implements I<%= props.name %>UseCase {
    execute(): Promise<void> {
        throw new Error('Method not implemented.');
    }
}